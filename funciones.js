//declaracion de funcion 
function suma(numUno, numDos) {
    return numUno + numDos
}
//invocando funcion
suma(2, 3)

//crear una funcion llamada esMayor, que reciba dos parametros
//y utilizando un condicional, comparar que numero es mayor 

function esMayor(numUno, numDos) {
    if (numUno > numDos) {
        console.log('numUno es mayor a numDos')
      }  else {
            console.log('numDos es mayor a numUno')
        }

        return numUno > numDos
    }
    esMayor(8, 23)


    